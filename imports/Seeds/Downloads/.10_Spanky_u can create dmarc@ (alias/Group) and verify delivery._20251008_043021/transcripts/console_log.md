# Console Log (paste key PowerShell runs here)
- Include commit/sweep blocks and notable errors (e.g., -replace / -Encoding issues).
- Keep order roughly chronological; this file is optional but recommended.